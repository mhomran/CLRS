#include "sorted_array_set.hpp"

/**
 * @brief Construct a new Sorted Array Set:: Sorted Array Set object
 * 
 */
SortedArraySet::SortedArraySet() {

}

/**
 * @brief Destroy the Sorted Array Set:: Sorted Array Set object
 * 
 */
SortedArraySet::~SortedArraySet() {

}

/**
 * @brief given an iterable X, build set from items in X
 * 
 * @param X 
 * @param size 
 */
void SortedArraySet::Build(int X[], int size){}

/**
 * @brief return the stored item with key k
 * 
 * @param k 
 */
void SortedArraySet::Find(int k){}

/**
 * @brief add x to set (replace item with key x.key if one already exists) 
 * 
 * @param k 
 */
void SortedArraySet::Insert(int k){}

/**
 * @brief remove and return the stored item with key k
 * 
 * @param k 
 */
void SortedArraySet::Delete(int k){}

/**
 * @brief return the stored item with smallest key
 * 
 */
void SortedArraySet::FindMin(void){}

/**
 * @brief return the stored item with largest key
 * 
 */
void SortedArraySet::FindMax(void){}

/**
 * @brief return the stored item with smallest key larger than k
 * 
 * @param k 
 */
void SortedArraySet::FindNext(int k){}

/**
 * @brief return the stored item with largest key smaller than k
 * 
 * @param k 
 */
void SortedArraySet::FindPrev(int k){}

/**
 * @brief Print the Set
 * 
 */
void SortedArraySet::Print(void){}